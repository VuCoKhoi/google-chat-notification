name: Test action
on: [push]
jobs:
  my_job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Get commit hash
        if: ${{ always() }}
        run: echo "::set-output name=sha8::$(echo ${GITHUB_SHA} | cut -c1-8)"
        id: slug
      - name: Chat Setup
        if: ${{ always() }}
        uses: ./ # Uses an action in the root directory
        with:
          project: ${{ github.repository }}
          commit: ${{ steps.slug.outputs.sha8 }}
          branch: ${{ github.ref }}
          status: ${{ job.status }}
          actionid: ${{ github.repository }}/runs/${{ github.run_id }}
          webhook: https://chat.googleapis.com/v1/spaces/AAAAKX5bk3A/messages?key=AIzaSyDdI0hCZtE6vySjMm-WEfRq3CPzqKqqsHI&token=21ZROh_UY9GWE9eiYW3-0xA_Ly5mE8tndtnmk4EjmYA%3D
